export default {
  // dateCreated: epoch timestamp (system field)
  // dateCompleted: optional field (will be null until complete)
  description: 'Follow up with Gelb on database permissions needed',
  // this is the user id of the associated user, not necessarily who created it
  userId: '1',
  // this is how the record is tied back to that level 10 series, since some team members will be a part of multiple
  meetingSeriesId: '1',
}